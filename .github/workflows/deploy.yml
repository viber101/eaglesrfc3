name: Build and Deploy to Hostinger VPS

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    env:
      VPS_HOST: 72.60.83.198
      VPS_USER: root
      VPS_PORT: 22

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Install dependencies
        run: npm install

      - name: Build project
        run: npm run build
        env:
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}
          VITE_POLL_KEY: ${{ secrets.VITE_POLL_KEY }}

      - name: Preflight VPS SSH diagnostics
        run: |
          set -euo pipefail
          echo "Runner public IP:"
          curl -s https://api.ipify.org || true
          echo
          echo "Testing TCP reachability to ${VPS_HOST}:${VPS_PORT}..."
          for i in 1 2 3; do
            if timeout 10 bash -c "cat < /dev/null > /dev/tcp/${VPS_HOST}/${VPS_PORT}"; then
              echo "TCP check passed on attempt ${i}"
              exit 0
            fi
            echo "TCP check failed on attempt ${i}"
            sleep 2
          done
          echo "TCP preflight failed after 3 attempts"
          exit 1

      - name: Prepare VPS directories
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ env.VPS_HOST }}
          username: ${{ env.VPS_USER }}
          port: ${{ env.VPS_PORT }}
          password: ${{ secrets.VPS_PASSWORD }}
          timeout: 180s
          debug: true
          script: |
            DEPLOY_DIR="$HOME/eaglesrfc"
            mkdir -p "$DEPLOY_DIR/dist" "$DEPLOY_DIR/api"
            rm -rf "$DEPLOY_DIR/dist/"*
            rm -rf "$DEPLOY_DIR/api/"*
            mkdir -p /var/www/eaglesrfc/dist

      - name: Copy files to VPS
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ env.VPS_HOST }}
          username: ${{ env.VPS_USER }}
          port: ${{ env.VPS_PORT }}
          password: ${{ secrets.VPS_PASSWORD }}
          timeout: 180s
          debug: true
          source: "dist/,api/,docker-compose.yml"
          target: "~/eaglesrfc"
          strip_components: 0

      - name: Restart services on VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ env.VPS_HOST }}
          username: ${{ env.VPS_USER }}
          port: ${{ env.VPS_PORT }}
          password: ${{ secrets.VPS_PASSWORD }}
          timeout: 180s
          debug: true
          script: |
            DEPLOY_DIR="$HOME/eaglesrfc"
            cd "$DEPLOY_DIR"
            cp -a "$DEPLOY_DIR/dist/." /var/www/eaglesrfc/dist/
            chown -R www-data:www-data /var/www/eaglesrfc/dist
            nginx -t
            systemctl reload nginx

            docker rm -f eaglesrfc_api || true
            docker compose up -d --build api
            docker compose ps
            docker logs eaglesrfc_api --tail 80 || true
